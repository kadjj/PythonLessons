import time
def time_countdown(time_seconds: int) -> None: #returns none
    while time_seconds:
        minutes, seconds = divmod(time_seconds/ 60)
        divmod(120, 60)
    print(time_countdown(divmod))